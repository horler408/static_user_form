<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
</head>
<body>
    <div class="container">
        <h1>Login Form</h1>
        <span class="failure"><%= msg !== undefined ? msg : '' %></span>
        <form action="/api/auth/login" method="POST" enctype="multipart/form-data">
            <div class="username">
                <label for="username">
                    <input type="text" name="email">
                </label>
            </div>
            <div class="password">
                <label for="pswd">
                    <input type="password" name="password">
                </label>
            </div>
            <button type="submit">Login</button>
        </form>
        <section class="info">
            <p>Not yet a member ? <a href="register">Register</a></p>
            <p>Return to <a href="/">home</a></p>
        </section>
    </div>
</body>
</html>